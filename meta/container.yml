---
# Add your Ansible Container service definitions here.
# For example:
#
apel-server:
  image: mysql:5.7
  ports:
    - "3306:3306"
  command: ['mysqld']
  dev_overrides:
    environment:
      - "DEBUG=1"
